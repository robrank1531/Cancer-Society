@model Capstone.Web.Models.PlanModel

@{
    ViewBag.Title = "CreatePlan";
}
<div class="container">
    <div class="panel panel-default">
        <div class="panel-heading" style="background-color: lightpink">
            <h2>Create A Plan</h2>
        </div>

        <div class="panel-body">
            @using (Html.BeginForm("CreatePlan", "Plan", FormMethod.Post))
            {
                @Html.AntiForgeryToken()
                <div>
                    <div class="form-group">
                        @Html.LabelFor(model => model.PlanName, "Plan Name", htmlAttributes: new { @class = "control-label col-md-2" })
                        <div class="col-md-10">
                            @Html.EditorFor(model => model.PlanName, new { htmlAttributes = new { @class = "form-control" } })
                            @Html.ValidationMessageFor(model => model.PlanName, "", new { @class = "text-danger" })
                        </div>
                    </div>

                    <div class="form-group">
                        <div class="">
                            <input type="submit" value="Create" class="btn btn-default buttonbox" />
                        </div>
                    </div>
                </div>
            }

            <div>
                @Html.ActionLink("Back to List", "Index")
            </div>
        </div>
    </div>
</div>
